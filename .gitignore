node_modules/
dist/
.build-mac/
dist-mac/
.env
*.log
.DS_Store

# npm link artifacts (repo-local bin/ and lib/ self-link)
bin/
lib/

# Local overrides that may contain real endpoints / secrets must not be committed.
config/local*.yaml
config/mcp.user.yaml
config/llm.user.yaml
config/search.user.yaml

# Build stamp (generated by scripts/stamp-build.mjs on every build; carries build time, not secrets).
public/build-info.js

# Local agent workspace state
.zcode/

# 架构图模板探针产物（视觉迭代用，不入库）
scripts/.probe/
